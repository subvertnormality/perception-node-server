version: '2'
services:
    web:
        build: .
        ports:
            - "3000:3000"
        links:
            - redis
        restart: always
        environment:
            - ADDR=http://138.197.18.129:3000/
    redis:
        image: redis
        ports:
            - "6379"