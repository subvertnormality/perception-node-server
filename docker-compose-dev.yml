version: '2'
services:
    web:
        build: .
        ports:
            - "3000:3000"
        links:
            - redis
        restart: always
        volumes:
            - ./
    redis:
        image: redis
        ports:
            - "6379"