version: '2'
services:
    web:
        build: .
        ports:
            - "3000:3000"
        links:
            - redis
        restart: always
        environment:
            - ADDR=http://127.0.0.1:3000/
    redis:
        image: redis
        ports:
            - "6379"